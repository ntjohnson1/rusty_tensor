[package]
name = "tensor-ext"
version = "0.1.0"
edition = "2021"

[lib]
name = "tensor_ext"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module", "abi3-py38"] }
numpy = "0.20"
rusty_tensor = { path = ".." }

[workspace]
